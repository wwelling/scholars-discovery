package edu.tamu.scholars.discovery.view.model;

/**
 * {@link CollectionView} enumeration for which `Layout` the collection view is to be contained in.
 */
public enum Layout {
    CONTAINER, GRID, LIST
}
